<ul class="dropdown-menu" style="padding: 15px;min-width: 250px;">
    <li>
        <div class="row">
            <div class="col-md-12">
                <form class="form navbar-login-form" role="form" method="post" action="{{ path("fos_user_security_check") }}" accept-charset="UTF-8" id="navbar-login-form">
                    <div class="form-group">
                        <label class="sr-only" for="navbar-login-email">{{ 'security.login.username'|trans({}, 'FOSUserBundle') }}</label>
                        <input type="email" name="_username" class="form-control" id="navbar-login-email" placeholder="{{ 'security.login.username'|trans({}, 'FOSUserBundle') }}" required>
                    </div>
                    <div class="form-group">
                        <label class="sr-only" for="navbar-login-password">{{ 'security.login.password'|trans({}, 'FOSUserBundle') }}</label>
                        <input type="password" name="_password" class="form-control" id="navbar-login-password" placeholder="{{ 'security.login.password'|trans({}, 'FOSUserBundle') }}" required>
                    </div>
                    <div class="checkbox">
                        <label><input type="checkbox" name="_remember_me" value="on"> {{ 'security.login.remember_me'|trans({}, 'FOSUserBundle') }}</label>
                    </div>
                    <div class="form-group">
                        <button type="submit" name="_submit" class="btn btn-success btn-block">{{ 'security.login.submit'|trans({}, 'FOSUserBundle') }}</button>
                    </div>
                    <input type="hidden" name="_csrf_token" value="{{ fos_csrf_provider.generateCsrfToken('authenticate') }}" />
                </form>
            </div>
        </div>
    </li>
<li class="divider"></li>
<li>
    <input class="btn btn-primary btn-block" type="button" id="sign-in-google" value="Sign In with Google">
    <input class="btn btn-primary btn-block" type="button" id="sign-in-twitter" value="Sign In with Twitter">
</li>
</ul>